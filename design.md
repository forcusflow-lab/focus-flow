# Focus Flow — モバイル設計方針

## 目的と設計原則

Focus Flowは、気が散る行動を避けるための**意図的な集中時間**と、毎日の**Todo実行**、継続を可視化する**習慣トラッキング**を一つの軽量なワークフローにまとめるローカルファーストのモバイルアプリである。添付APKから確認できた「集中開始・時間設定・予定・進捗」といった生産性の観点を参考にするが、画面・コード・ブランドを複製せず、独自の情報設計で構成する。

全画面はiPhoneの縦向き9:16を基準とする。一手で届きやすい画面下部に主要タブと作成アクションを置き、最も多い操作である「Todoを完了する」「習慣を記録する」「集中を開始する」を1〜2タップで完結させる。Apple Human Interface Guidelinesに沿い、十分な余白、44pt以上のタップ領域、明確な階層、動作を伴うフィードバックを用いる。

## 画面一覧

| 画面 | 主な内容 | 主な操作 |
|---|---|---|
| 今日 | 日付、アプリ制限の状態、必須項目の進捗、必須・通常を区別したTodo／習慣の連続リスト | Todo完了、習慣記録、Todo追加、集中制限の確認 |
| Todo | 未完了／完了の切替、優先度、期限、必須・通常バッジ、進捗 | 追加、編集、完了、削除、フィルター |
| 習慣 | 習慣ごとの連続日数、今週の達成状況、曜日別チェック | 追加、今日の記録、編集、削除 |
| メモ | すぐに残すアイデアのカード一覧、Todo化導線、キーボード安全な入力シート | 追加、編集、Todo化、削除 |
| 振り返り | 直近7日間の集中時間、Todo達成、習慣達成、継続状況 | 週の状況確認 |
| 集中制限 | 制限のオン／オフ、厳格モード、選択済みアプリ、Android権限、時間帯 | 制限開始、厳格化、制限アプリの選択、状態の再確認 |
| ウィジェット設定 | Todoと習慣を一つにまとめたウィジェットの見た目 | 透明度、背景、アクセント、文字サイズの選択 |
| 作成・編集シート | Todo／習慣／メモの入力、必須・通常の選択、優先度・期限・頻度・目標時間 | 保存、取り消し |

## 主要フロー

| ユーザーの目的 | 操作の流れ | 完了時のフィードバック |
|---|---|---|
| 今日の必須項目を終える | 今日 → Todoまたは習慣カードのチェックをタップ | 成功ハプティクス、残数・解除状態の即時更新 |
| 集中作業を始める | 今日 → 「集中を開始」→ 25分を開始／一時停止／完了 | タイマー表示、完了後に集中時間へ加算 |
| 新しいTodoを追加する | Todo → 追加ボタン → 内容・必須／通常・優先度・期限 → 保存 | 未完了一覧の先頭に追加し、無料上限は未完了件数で評価 |
| 習慣を継続する | 習慣 → 当日の丸いチェックをタップ | 連続日数と今週の達成状況を更新 |
| 週の行動を振り返る | 振り返り → 7日間の指標と習慣カードを確認 | 今日の次の改善行動を把握 |

## ローカルデータモデル

| エンティティ | 主なフィールド | 永続化の意図 |
|---|---|---|
| Todo | id、title、priority、dueDate、completed、createdAt | 今日のタスクと達成率を構成する |
| Habit | id、title、color、goalPerWeek、completedDates、createdAt | 継続日数と週間達成を計算する |
| FocusSession | id、startedAt、durationMinutes、completed | 集中時間と週次推移を記録する |
| AppState | todos、habits、sessions、lastOpenedAt | AsyncStorageへ単一のJSONとして保存する |

## 2026年8月 UX再設計

すべての画面で、**カードの外枠を重ねて情報を分断しない**。同じ目的の項目は一枚のセクションカード内に罫線で連続表示し、異なる目的のセクションだけを16〜20ptの余白で区切る。Todoと習慣は同じ行の構造を共有し、左から「完了操作」「種別・必須性」「タイトルと補助情報」「必要な場合だけ進捗または編集」とする。必須は森林緑の小さなラベル、通常は青灰色の小さなラベルで区別し、色だけに依存しない。

| 領域 | 新しいレイアウト | 操作上の意図 |
|---|---|---|
| 今日 | 円形ゲージと「まず取り組む1件」を廃止し、残数・完了数を1行で示す進捗カードの下に、必須・通常を混在させた連続リストを配置する | 項目の重複をなくし、次に何をするかをカードの順序で自然に読めるようにする |
| Todo | 「フォーカス」のタブを廃止し、「未完了」「完了」の2タブに限定する。件数はラベルの補足として右寄せし、タブ内の不要な数字をなくす | タブの役割を一般的なTodoアプリと同じにし、迷いを減らす |
| メモ入力 | シート高を画面に収め、フォーム本体をスクロール可能にする。Androidではシステムのソフト入力領域に合わせてシートを持ち上げる | タイトル、本文、保存がキーボードと重ならず片手で完結する |
| 厳格モード | 状態を「保護中」と明記し、終了確認では「今すぐ終了」と「保護を続ける」を意味が逆にならない順序で表示する。制約は簡潔な補足へ退避する | ユーザーが本当に保護を終了する操作なのかを、安心して判断できるようにする |
| 制限アプリ | 選択済みアプリを常時先頭に表示し、残りの候補は検索・全件一覧で選ぶ | 設定済みの対象を見失わず、端末内アプリが欠けない |
| 統合ウィジェット | 1つの大きなウィジェットだけを提供する。最大4行のTodo／習慣を表示し、各行に必須／通常ラベル、完了チェック、タイトルを置く | ホーム画面だけで今日の行動を確認・完了でき、複数ウィジェットを選ぶ負担をなくす |

ウィジェットは、透明度を0%、25%、50%の3段階で変更できる。背景とアクセントは一括で選び、行の情報密度はAndroidのホーム画面で読める量に制限する。時間管理中の項目は、残り時間を表示して完了操作を無効にする。

## 視覚設計

ブランドは「静かな集中と小さな前進」を表現する。明るい背景は**霧白 #F7F8F5**、主要なアクションは**森林緑 #246B5A**、集中セッションは**深い青 #315B8C**、達成は**若草 #4B9B72**、注意は**琥珀 #C98626**とする。カードは白を基調に12〜20ptの角丸と薄い境界線を用い、数値には強いコントラストを置く。赤い警告色は削除など取り返しのつかない操作に限定する。

## 実装境界

初期版はユーザー認証・クラウド同期を扱わない。すべての情報は端末内に保存し、ネットワーク接続なしでもTodo、習慣、メモ、振り返りを利用できる。Android版では、ユーザーが明示的にアクセシビリティ権限を許可した場合だけ、選択したアプリの前面化を検出して集中制限を適用する。画面内容・メッセージ・入力内容・スクリーンショットは読み取らない。

厳格モードは、Focus Flow内の通常操作から制限を外せないようにする保護である。Androidのシステム設定、強制停止、アプリ削除を通常アプリが防げないことは明示し、これらを防げるかのような表現は使わない。
