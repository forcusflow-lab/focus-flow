import { useMemo } from "react";
import { StyleSheet, TouchableOpacity, View } from "react-native";
import MaterialIcons from "@expo/vector-icons/MaterialIcons";

import { HabitProgressControl } from "@/components/focus-flow/habit-progress-control";
import { ScaledText as Text } from "@/components/focus-flow/scaled-text";
import { COLORS, Pill, useFocusPalette } from "@/components/focus-flow/ui";
import type { Habit, Todo } from "@/lib/focus-flow/types";
import { dayKey, dayKeyOffset, formatJapaneseDate, getTodoDueStatus, getTodoSubtasks, habitStreak, isHabitCompleteOn, isTodoAchieved, shortWeekday, todoProgressLabel, weeklyHabitProgress } from "@/lib/focus-flow/utils";

type Translate = (ja: string, en: string) => string;

export function TodoItemCard({ todo, showRequired = todo.isRequired, language, t, onOpen, onToggle, onProgress, onSubtask, onDelete }: { todo: Todo; showRequired?: boolean; language: "ja" | "en"; t: Translate; onOpen: () => void; onToggle: () => void; onProgress: (delta: number) => void; onSubtask: (id: string) => void; onDelete?: () => void }) {
  const achieved = isTodoAchieved(todo); const dueStatus = getTodoDueStatus(todo); const subtasks = getTodoSubtasks(todo); const progress = todoProgressLabel(todo, language); const priorityColor = todo.priority === "high" ? COLORS.error : todo.priority === "medium" ? COLORS.warning : COLORS.blue;
  const palette = useFocusPalette();
  const due = !todo.dueDate ? undefined : dueStatus === "overdue" ? t("期限超過", "Overdue") : dueStatus === "today" ? t("今日が期限", "Due today") : formatJapaneseDate(todo.dueDate, language);
  return <View style={[styles.todoRow, { backgroundColor: achieved ? palette.elevated : palette.surface, borderColor: palette.border }]}><View style={[styles.rail, { backgroundColor: priorityColor }]} /><TouchableOpacity accessibilityRole="checkbox" accessibilityState={{ checked: achieved }} accessibilityLabel={t(`「${todo.title}」を完了にする`, `Mark “${todo.title}” complete`)} onPress={(event) => { event.stopPropagation(); onToggle(); }} style={styles.checkTouchTarget}><View style={[styles.check, { borderColor: palette.border }, achieved && styles.checkDone]}>{achieved ? <MaterialIcons name="check" size={17} color={COLORS.white} /> : null}</View></TouchableOpacity><View style={styles.copy}><TouchableOpacity accessibilityRole="button" onPress={onOpen} activeOpacity={0.72}><Text style={[styles.title, achieved && styles.titleDone]} numberOfLines={2}>{todo.title}</Text><View style={styles.meta}>{showRequired ? <View style={styles.requiredPillSlot}><Pill label={t("必須", "Must-do")} color={COLORS.forest} /></View> : null}{due || subtasks.length ? <View style={styles.metaDetails}>{due ? <Text style={[styles.due, dueStatus === "overdue" && styles.overdue]}>{due}</Text> : null}{subtasks.length ? <Text style={styles.metaText}>{subtasks.filter((subtask) => subtask.completed).length}/{subtasks.length} {t("手順", "steps")}</Text> : null}</View> : null}</View></TouchableOpacity>{progress ? <View style={[styles.progressControl, { backgroundColor: palette.elevated }]}><TouchableOpacity accessibilityLabel={t("進捗を減らす", "Decrease progress")} hitSlop={7} onPress={() => onProgress(-1)} style={[styles.progressButton, { backgroundColor: palette.surface }]}><MaterialIcons name="remove" size={15} color={palette.primary} /></TouchableOpacity><Text style={[styles.progressText, { color: palette.primary }]}>{progress}</Text><TouchableOpacity accessibilityLabel={t("進捗を増やす", "Increase progress")} hitSlop={7} onPress={() => onProgress(1)} style={[styles.progressButton, { backgroundColor: palette.surface }]}><MaterialIcons name="add" size={15} color={palette.primary} /></TouchableOpacity></View> : null}{subtasks.length ? <View style={styles.subtasks}>{subtasks.slice(0, 2).map((subtask) => <TouchableOpacity key={subtask.id} accessibilityRole="checkbox" accessibilityState={{ checked: subtask.completed }} onPress={() => onSubtask(subtask.id)} style={styles.subtask}><View style={[styles.subtaskCheck, { borderColor: palette.border }, subtask.completed && styles.subtaskCheckDone]}>{subtask.completed ? <MaterialIcons name="check" size={10} color={COLORS.white} /> : null}</View><Text style={[styles.subtaskText, subtask.completed && styles.subtaskTextDone]} numberOfLines={1}>{subtask.title}</Text></TouchableOpacity>)}{subtasks.length > 2 ? <TouchableOpacity onPress={onOpen}><Text style={styles.moreSteps}>+{subtasks.length - 2} {t("件の手順", "more steps")}</Text></TouchableOpacity> : null}</View> : null}</View>{onDelete ? <TouchableOpacity accessibilityLabel={t("Todoを削除", "Delete task")} hitSlop={8} onPress={onDelete} style={styles.trailing}><MaterialIcons name="delete-outline" size={20} color={palette.muted} /></TouchableOpacity> : <TouchableOpacity accessibilityLabel={t("Todoの詳細を開く", "Open task details")} hitSlop={8} onPress={onOpen} style={styles.trailing}><MaterialIcons name="chevron-right" size={21} color={palette.muted} /></TouchableOpacity>}</View>;
}

export function HabitItemCard({ habit, showRequired = habit.isRequired, language, t, onOpen, onToggle, onStartTimer, onPauseTimer, onProgress, onDelete }: { habit: Habit; showRequired?: boolean; language: "ja" | "en"; t: Translate; onOpen: () => void; onToggle: (date?: string) => void; onStartTimer: () => void; onPauseTimer: () => void; onProgress: (delta: number) => void; onDelete?: () => void }) {
  const today = dayKey(); const done = isHabitCompleteOn(habit, today); const weekly = weeklyHabitProgress(habit); const week = useMemo(() => Array.from({ length: 7 }, (_, index) => dayKeyOffset(index - 6)), []);
  const palette = useFocusPalette();
  return <View style={[styles.habitRow, { backgroundColor: done ? palette.elevated : palette.surface, borderColor: palette.border }]}><View style={[styles.rail, { backgroundColor: habit.color }]} /><TouchableOpacity accessibilityRole="checkbox" accessibilityState={{ checked: done }} accessibilityLabel={t(`「${habit.title}」を今日の習慣として記録`, `Record “${habit.title}” for today`)} onPressIn={(event) => event.stopPropagation()} onPress={() => onToggle()} style={styles.checkTouchTarget}><View style={[styles.check, { borderColor: palette.border }, done && { backgroundColor: habit.color, borderColor: habit.color }]}>{done ? <MaterialIcons name="check" size={17} color={COLORS.white} /> : null}</View></TouchableOpacity><View style={styles.copy}><TouchableOpacity accessibilityRole="button" onPress={onOpen}><Text style={[styles.title, done && styles.titleDone]} numberOfLines={1}>{habit.title}</Text><View style={styles.meta}>{showRequired ? <View style={styles.requiredPillSlot}><Pill label={t("必須", "Must-do")} color={COLORS.forest} /></View> : null}<Text style={styles.metaText}>{t(`週 ${weekly.completed}/${weekly.target}`, `${weekly.completed}/${weekly.target} this week`)} · {habitStreak(habit)}{t("日連続", "-day streak")}</Text></View></TouchableOpacity><View style={styles.weekRow}>{week.map((key) => { const marked = isHabitCompleteOn(habit, key); const current = key === today; return <TouchableOpacity key={key} accessibilityLabel={t(`${shortWeekday(key)}曜日を記録`, `Record ${shortWeekday(key, language)}`)} hitSlop={7} onPress={() => onToggle(key)} style={[styles.dayDot, { borderColor: palette.border }, current && { borderColor: habit.color }, marked && { backgroundColor: habit.color, borderColor: habit.color }]}>{marked ? <MaterialIcons name="check" size={10} color={COLORS.white} /> : <Text style={[styles.dayLetter, current && { color: habit.color }]}>{shortWeekday(key, language)}</Text>}</TouchableOpacity>})}</View><HabitProgressControl habit={habit} date={today} language={language} onAdjust={onProgress} onStartTimer={onStartTimer} onPauseTimer={onPauseTimer} /></View>{onDelete ? <TouchableOpacity accessibilityLabel={t("習慣を削除", "Delete habit")} hitSlop={8} onPress={onDelete} style={styles.trailing}><MaterialIcons name="delete-outline" size={20} color={palette.muted} /></TouchableOpacity> : <TouchableOpacity accessibilityLabel={t("習慣の詳細を開く", "Open habit details")} hitSlop={8} onPress={onOpen} style={styles.trailing}><MaterialIcons name="chevron-right" size={21} color={palette.muted} /></TouchableOpacity>}</View>;
}

const styles = StyleSheet.create({
  todoRow: { position: "relative", minHeight: 70, flexDirection: "row", alignItems: "flex-start", borderWidth: 1, borderRadius: 15, paddingVertical: 9, paddingLeft: 14, paddingRight: 4, marginBottom: 6, overflow: "hidden" },
  habitRow: { position: "relative", minHeight: 98, flexDirection: "row", alignItems: "flex-start", borderWidth: 1, borderRadius: 15, paddingVertical: 9, paddingLeft: 14, paddingRight: 4, marginBottom: 6, overflow: "hidden" },
  rail: { position: "absolute", left: 0, top: 0, bottom: 0, width: 4 },
  checkTouchTarget: { width: 44, height: 44, alignItems: "center", justifyContent: "center", marginLeft: -8, marginTop: -5, marginRight: 2 },
  check: { width: 28, height: 28, borderRadius: 6, borderWidth: 1.5, alignItems: "center", justifyContent: "center" }, checkDone: { borderColor: COLORS.success, backgroundColor: COLORS.success },
  copy: { flex: 1, minWidth: 0, overflow: "visible" }, title: { color: COLORS.text, fontSize: 15, lineHeight: 21, fontWeight: "800" }, titleDone: { color: "#75827C", textDecorationLine: "line-through", textDecorationColor: "#75827C" },
  meta: { width: "100%", minHeight: 34, flexDirection: "column", alignItems: "flex-start", gap: 4, marginTop: 4, overflow: "visible" }, requiredPillSlot: { minWidth: 84, minHeight: 34, alignSelf: "stretch", alignItems: "flex-start", justifyContent: "center", overflow: "visible" }, metaDetails: { flexDirection: "row", flexWrap: "wrap", alignItems: "center", gap: 6 }, due: { color: COLORS.muted, fontSize: 11, lineHeight: 16, fontWeight: "800" }, overdue: { color: COLORS.error }, metaText: { width: "100%", color: COLORS.muted, fontSize: 11, lineHeight: 16, fontWeight: "700" },
  progressControl: { alignSelf: "flex-start", flexDirection: "row", alignItems: "center", gap: 7, borderRadius: 10, marginTop: 6, paddingHorizontal: 5, paddingVertical: 3 }, progressButton: { width: 25, height: 25, alignItems: "center", justifyContent: "center", borderRadius: 7 }, progressText: { minWidth: 48, textAlign: "center", fontSize: 11, fontWeight: "900" },
  subtasks: { marginTop: 6, gap: 4 }, subtask: { minHeight: 26, flexDirection: "row", alignItems: "center", gap: 6 }, subtaskCheck: { width: 16, height: 16, borderRadius: 5, alignItems: "center", justifyContent: "center", borderWidth: 1 }, subtaskCheckDone: { borderColor: COLORS.success, backgroundColor: COLORS.success }, subtaskText: { flex: 1, color: COLORS.muted, fontSize: 11, fontWeight: "700" }, subtaskTextDone: { textDecorationLine: "line-through" }, moreSteps: { color: COLORS.forest, fontSize: 11, fontWeight: "800", marginLeft: 22 },
  weekRow: { flexDirection: "row", gap: 6, marginTop: 6 }, dayDot: { width: 22, height: 22, borderRadius: 11, borderWidth: 1, alignItems: "center", justifyContent: "center" }, dayLetter: { color: COLORS.muted, fontSize: 9, fontWeight: "800" }, trailing: { width: 34, height: 38, alignItems: "center", justifyContent: "center", marginLeft: 2 },
});
